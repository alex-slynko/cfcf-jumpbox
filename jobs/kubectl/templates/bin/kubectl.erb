#!/usr/bin/env bash

set -e

/var/vcap/packages/kubectl/bin/kubectl --token="<%= link('kube-apiserver').p('admin-password') %>" --server=https://master.cfcr.internal:8443 --certificate-authority="/var/vcap/jobs/kubectl/config/ca.pem" $@
